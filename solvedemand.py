import networkx as nx
import matplotlib.pyplot as plt

# Build a sample network of locations containing  items

# Get a list of items which have to be fulfilled
 
# Check if item if present in the quantity at location

# If available, then solved

# If not, check if that location is connected to any other location

# In each location, find if that material is available

# Get back all the branches which can give quantity

# Choose the shortest/lowest cost branch